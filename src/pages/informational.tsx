import clsx from 'clsx';
import Layout from '@theme/Layout';

import styles from './index.module.css';

export default function Informational(): JSX.Element {
    return (
        <Layout
          title={`Informational`}
          description="Subjektify Improvement Proposals">
          <main>
          </main>
        </Layout>
    );
}
